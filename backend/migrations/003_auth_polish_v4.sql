-- TelePlay auth polish v4 migration.
-- No schema changes are required for v4. This file exists so the migration
-- runner can record that the v4 migration set was checked/applied.

BEGIN;
SELECT 1;
COMMIT;
